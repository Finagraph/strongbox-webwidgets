import * as React from 'react';

// The circle for the bullet is 50 wide and there's a padding of 16 on the right.
export const bulletWidth = '66px';

export const bulletStyle: React.CSSProperties = {
    width: bulletWidth,
};

